This is example code showing how to use Facebook's Graph API. See the wiki for further details.

The site, if loaded as a Facebook app, will display 'articles', upon which you can create mock comments by clicking a button (this will come later). Like buttons are included (but may not show: need to investigate), and the app should cause opengraph messages upon viewing an 'article'
